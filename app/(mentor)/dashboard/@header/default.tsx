import { Appbar } from "@/components/modules/mentor-dashboard/dashboard-header";
import React from "react";

const DefaultHeader: React.FC = () => {
  return (
    <header>
      <Appbar />
    </header>
  );
};

export default DefaultHeader;
